spring:
  datasource:
    url: jdbc:mysql://localhost:3306/UserDB?useSSL=false&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
  security:
    user:
      name: user
      password: password

